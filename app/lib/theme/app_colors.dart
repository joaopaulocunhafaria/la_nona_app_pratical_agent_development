import 'package:flutter/material.dart';

/// Centraliza a paleta de cores oficial definida em COLORS.md.
abstract final class AppColors {
  // Primary
  static const Color primaryDark = Color(0xFF1E3A1F);
  static const Color primaryBase = Color(0xFF254D27);
  static const Color primaryLight = Color(0xFF3B6B3E);

  // Secondary
  static const Color secondaryDark = Color(0xFF163017);
  static const Color secondaryBase = Color(0xFF2F5D33);
  static const Color secondaryLight = Color(0xFF5C8F61);

  // Background
  static const Color backgroundMain = Color(0xFFF5F6F2);
  static const Color backgroundSoft = Color(0xFFE9EFE6);
  static const Color backgroundDarkMode = Color(0xFF0F1F10);

  // Surface
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceSoftGreen = Color(0xFFEEF4EC);
  static const Color surfaceDark = Color(0xFF1A2B1B);

  // Accent
  static const Color accentCream = Color(0xFFF2EFE6);
  static const Color accentGoldSoft = Color(0xFFD4B97A);

  // Text
  static const Color textPrimary = Color(0xFF0F1F10);
  static const Color textSecondary = Color(0xFF4B5A4C);
  static const Color textOnDark = Color(0xFFF5F6F2);
}
